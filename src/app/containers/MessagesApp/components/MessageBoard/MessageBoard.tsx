import * as React from 'react';

import * as styles from './MessageBoard.css';

export default class MessageBoard extends React.PureComponent {
    render() {
        return (
            <div className={styles.messageapp__messageboard}>Test</div>
        );
    }
}